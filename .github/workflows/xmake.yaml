name: Build
permissions:
  contents: write
on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  VULKAN_VERSION: 1.4.309.0
  LLVM_VERSION: 20
  LLVM_WINDOWS_VERSION: 20.1.4
  LLVM_MACOS_VERSION: 22

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        platform:
          # `release: true` marks the one configuration per platform that ships in a GitHub release.
          # `xmake_plat` is xmake's own platform name, which is what the build directory is keyed on.
          - { runner: windows-2025, os: windows, arch: x64,    toolchain: msvc,     mode: release, runtime: MT,  xmake_plat: windows, release: true }
          - { runner: windows-2025, os: windows, arch: x64,    toolchain: msvc,     mode: debug,   runtime: MTd, xmake_plat: windows }

          # - { runner: windows-2025, os: windows, arch: x64,    toolchain: clang-cl, mode: release, runtime: MT,  xmake_plat: windows }
          # - { runner: windows-2025, os: windows, arch: x64,    toolchain: clang-cl, mode: debug,   runtime: MTd, xmake_plat: windows }

          - { runner: ubuntu-latest,  os: linux,   arch: x86_64, toolchain: clang-20, mode: release, runtime: c++_static, xmake_plat: linux, release: true }
          - { runner: ubuntu-latest,  os: linux,   arch: x86_64, toolchain: clang-20, mode: debug,   runtime: c++_static, xmake_plat: linux }

          - { runner: macos-26,  os: macos,   arch: arm64, toolchain: mac-clang, mode: release, runtime: c++_static, xmake_plat: macosx, release: true }
          - { runner: macos-26,  os: macos,   arch: arm64, toolchain: mac-clang, mode: debug,   runtime: c++_static, xmake_plat: macosx }

    runs-on: ${{ matrix.platform.runner }}
    steps:
      - name: Get current date as package key
        id: cache_key
        run: echo "key=$(date +'%W')" >> $GITHUB_OUTPUT
        shell: bash

      - name: Set OUTPUT_FILE variable
        run: echo "OUTPUT_FILE=${{ matrix.platform.os }}_${{ matrix.platform.arch }}_${{ matrix.platform.mode }}_${{ matrix.platform.toolchain }}_${{ matrix.platform.runtime }}${{ matrix.platform.archive_ext }}" >> $GITHUB_ENV
        shell: bash

      - name: Set package name
        run: echo "PACKAGE_NAME=Rogylus-${{ matrix.platform.os }}-${{ matrix.platform.arch }}" >> $GITHUB_ENV
        shell: bash

      # Derived from the matrix rather than asked of xmake: `xmake show -t Rogylus` exits non-zero on
      # the .rcss assets registered through install_resources, which trips `bash -e -o pipefail`.
      - name: Resolve target directory
        id: targetdir
        run: echo "dir=build/${{ matrix.platform.xmake_plat }}/${{ matrix.platform.arch }}/${{ matrix.platform.mode }}" >> $GITHUB_OUTPUT
        shell: bash

      # Force xmake to a specific folder (for cache)
      - name: Set xmake env
        run: echo "XMAKE_GLOBALDIR=${{ runner.workspace }}/xmake-global" >> $GITHUB_ENV
        shell: bash

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Linux dependencies
        if: ${{ matrix.platform.os == 'linux' }}
        run: |
          wget -O - https://apt.llvm.org/llvm-snapshot.gpg.key | sudo apt-key add -
          sudo add-apt-repository -y "deb http://apt.llvm.org/noble/ llvm-toolchain-noble-${{ env.LLVM_VERSION }} main"
          sudo add-apt-repository -y "deb-src http://apt.llvm.org/noble/ llvm-toolchain-noble-${{ env.LLVM_VERSION }} main"
          sudo apt update -y
          sudo apt install -y clang-${{ env.LLVM_VERSION }} lld-${{ env.LLVM_VERSION }} llvm-${{ env.LLVM_VERSION }} libllvm${{ env.LLVM_VERSION }} llvm-${{ env.LLVM_VERSION }}-runtime libclang-common-${{ env.LLVM_VERSION }}-dev libclang-${{ env.LLVM_VERSION }}-dev libclang1-${{ env.LLVM_VERSION }} libclang-rt-${{ env.LLVM_VERSION }}-dev libc++-${{ env.LLVM_VERSION }}-dev libc++abi-${{ env.LLVM_VERSION }}-dev libunwind-${{ env.LLVM_VERSION }}-dev libllvmlibc-${{ env.LLVM_VERSION }}-dev
          sudo apt install libx11-dev libxext-dev libxrandr-dev libxrender-dev libxfixes-dev libxi-dev libxinerama-dev libxkbcommon-dev

      - name: Set up Python
        if: ${{ matrix.platform.os == 'macos' }}
        uses: actions/setup-python@v6
        with:
          python-version: '3.14'

      - name: Install MacOS dependencies
        if: ${{ matrix.platform.os == 'macos' }}
        run: |
          python -m pip install --upgrade pip setuptools wheel

      - name: Install LLVM for Windows
        if: ${{ matrix.platform.os == 'windows' }}
        uses: KyleMayes/install-llvm-action@master
        with:
          version: "${{env.LLVM_WINDOWS_VERSION}}"

      - name: Install LLVM for MacOS
        if: ${{ matrix.platform.os == 'macos' }}
        run: |
          brew install llvm@${{ env.LLVM_MACOS_VERSION }}
          LLVM_PATH=$(brew --prefix llvm@${{ env.LLVM_MACOS_VERSION }})
          echo "LLVM_PATH=$LLVM_PATH" >> $GITHUB_ENV
          echo "PATH=$LLVM_PATH/bin:$PATH" >> $GITHUB_ENV
          echo "LDFLAGS=-L$LLVM_PATH/lib/c++ -Wl,-rpath,$LLVM_PATH/lib/c++ $LDFLAGS" >> $GITHUB_ENV
          echo "CPPFLAGS=-I$LLVM_PATH/include $CPPFLAGS" >> $GITHUB_ENV
          echo "CXXFLAGS=-stdlib=libc++ $CXXFLAGS" >> $GITHUB_ENV

      - name: Verify LLVM version
        if: ${{ matrix.platform.os == 'macos' }}
        run: |
          which clang++
          clang++ --version
          echo "LLVM_PATH: $LLVM_PATH"
          echo "LDFLAGS: $LDFLAGS"
          echo "CPPFLAGS: $CPPFLAGS"
          echo "CXXFLAGS: $CXXFLAGS"
          brew --prefix llvm@${{ env.LLVM_MACOS_VERSION }}

      # Install xmake
      - name: Setup xmake
        uses: xmake-io/github-action-setup-xmake@v1
        with:
          xmake-version: branch@dev
          actions-cache-folder: .xmake-cache-W${{ steps.cache_key.outputs.key }}

      # Update xmake repository (in order to have the file that will be cached)
      - name: Update xmake repository
        run: xmake repo --update -vD

      # Fetch xmake dephash
      - name: Retrieve dependencies hash
        id: dep_hash
        run: echo "hash=$(xmake l utils.ci.packageskey)" >> $GITHUB_OUTPUT
        shell: bash

      # Retrieve xmake dependencies
      - name: Restore cached xmake dependencies
        id: restore-depcache
        uses: actions/cache/restore@v4
        with:
          path: ${{ env.XMAKE_GLOBALDIR }}/.xmake/packages
          key: ${{ matrix.platform.os }}-${{ matrix.platform.arch }}-${{ matrix.platform.mode }}-${{ matrix.platform.runtime }}-${{ matrix.platform.toolchain }}-${{ steps.dep_hash.outputs.hash }}-W${{ steps.cache_key.outputs.key }}

      - name: Configure xmake and install dependencies
        run: xmake config -v --toolchain=${{ matrix.platform.toolchain }} --arch=${{ matrix.platform.arch }} --mode=${{ matrix.platform.mode }} --runtimes=${{ matrix.platform.runtime }} --yes --policies=package.precompiled:n

      # Save dependencies
      - name: Save cached xmake dependencies
        if: ${{ !steps.restore-depcache.outputs.cache-hit }}
        uses: actions/cache/save@v4
        with:
          path: ${{ env.XMAKE_GLOBALDIR }}/.xmake/packages
          key: ${{ steps.restore-depcache.outputs.cache-primary-key }}

      # rcli runs during the build (the compile_shaders rule) and dlopens slang's downstream
      # compilers, so its shared deps have to be discoverable before anything is built.
      - name: Stage package shared libs (Unix)
        if: ${{ matrix.platform.os != 'windows' }}
        run: |
          mkdir -p dest/bin
          find "${{ env.XMAKE_GLOBALDIR }}/.xmake/packages" \( -name "*.so*" -o -name "*.dylib" \) -exec cp {} dest/bin/ \; 2>/dev/null || true
          echo "LD_LIBRARY_PATH=$PWD/dest/bin:$LD_LIBRARY_PATH" >> $GITHUB_ENV
          echo "DYLD_LIBRARY_PATH=$PWD/dest/bin:$DYLD_LIBRARY_PATH" >> $GITHUB_ENV
        shell: bash

      - name: Stage package shared libs (Windows)
        if: ${{ matrix.platform.os == 'windows' }}
        run: |
          $dest = "${{ steps.targetdir.outputs.dir }}"
          New-Item -ItemType Directory -Force -Path $dest | Out-Null
          # xmake vendors build-time tools into the same packages tree, and they drag in a whole
          # CPython (python3xx, tcl/tk, sqlite3, openssl, its own zlib1). None of it is a runtime
          # dep of the game, so skip those packages by path -- filtering on DLL name would also
          # throw away a genuine zlib1.dll from the zlib package.
          $toolPackages = '\\packages\\(c\\cmake|m\\(meson|msys2)|n\\(nasm|ninja)|p\\(python|pkgconf|perl)|s\\strawberry-perl|7\\7z|g\\git|r\\ruby|y\\yasm)\\'
          Get-ChildItem -Path "${{ env.XMAKE_GLOBALDIR }}\.xmake\packages" -Recurse -Filter "*.dll" |
            Where-Object { $_.FullName -notmatch $toolPackages } |
            ForEach-Object { Copy-Item $_.FullName -Destination $dest -Force -ErrorAction SilentlyContinue }
        shell: pwsh

      - name: Build all targets
        run: xmake build -vD -a

      # The install rules drop Assets next to the binary, so the target dir is the playable game.
      # It needs the shared libs alongside it too, for a zip that runs without env vars.
      - name: Bundle shared libs next to the binary (Unix)
        if: ${{ matrix.platform.os != 'windows' }}
        run: |
          dir="${{ steps.targetdir.outputs.dir }}"
          test -d "$dir" || { echo "::error::expected build output at $dir"; ls -R build || true; exit 1; }
          find dest/bin -maxdepth 1 \( -name "*.so*" -o -name "*.dylib" \) -exec cp -n {} "$dir/" \; 2>/dev/null || true
        shell: bash

      - name: Install project targets
        run: |
          xmake install -vo dest/
          rm -rf dest/include/ dest/lib/
        shell: bash

      - name: Archive production artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.platform.os }}-${{ matrix.platform.arch }}-${{ matrix.platform.mode }}-${{ matrix.platform.toolchain }}-${{ matrix.platform.runtime }}
          path: |
            dest

      # --- Release packaging: a zip that runs straight out of the extracted folder ---
      - name: Package playable build (Linux)
        if: ${{ matrix.platform.release && matrix.platform.os == 'linux' }}
        run: |
          stage="package/${{ env.PACKAGE_NAME }}"
          mkdir -p "$stage"
          cp -R "${{ steps.targetdir.outputs.dir }}/." "$stage/"
          # Runtime leftovers from the build machine, not part of the game.
          rm -rf "$stage/logs" "$stage/context_config.toml" "$stage/compile_commands.json"
          # The resource compiler toolchain is staged for rcli during the build. The game never
          # loads it, so it must not reach the zip.
          find "$stage" -maxdepth 1 \( -name '*slang*' -o -name '*SPIRV-Tools*' -o -name '*spirv-tools*' \
            -o -name '*ResourceCompiler*' -o -name 'rcli' \) -delete
          # Only the compiled packs are read at runtime, the .slang sources are build inputs.
          find "$stage/Assets/Shaders" -mindepth 1 ! -name '*.oxpack' -delete
          chmod +x "$stage/Rogylus"
          cp README.md LICENSE "$stage/" 2>/dev/null || true
          cd package && zip -qr "../${{ env.PACKAGE_NAME }}.zip" "${{ env.PACKAGE_NAME }}"
        shell: bash

      - name: Package playable build (macOS)
        if: ${{ matrix.platform.release && matrix.platform.os == 'macos' }}
        run: |
          stage="package/${{ env.PACKAGE_NAME }}"
          mkdir -p "$stage"
          cp -R "${{ steps.targetdir.outputs.dir }}/." "$stage/"
          # Runtime leftovers from the build machine, not part of the game.
          rm -rf "$stage/logs" "$stage/context_config.toml" "$stage/compile_commands.json"
          # Finder litters these through any directory it has looked at.
          find "$stage" -name '.DS_Store' -delete
          # The resource compiler toolchain is staged for rcli during the build. The game never
          # loads it, so it must not reach the zip.
          find "$stage" -maxdepth 1 \( -name '*slang*' -o -name '*SPIRV-Tools*' -o -name '*spirv-tools*' \
            -o -name '*ResourceCompiler*' -o -name 'rcli' \) -delete
          # Only the compiled packs are read at runtime, the .slang sources are build inputs.
          find "$stage/Assets/Shaders" -mindepth 1 ! -name '*.oxpack' -delete
          chmod +x "$stage/Rogylus"
          cp README.md LICENSE "$stage/" 2>/dev/null || true
          # -X keeps resource forks and extra attributes out of the archive.
          cd package && zip -qrX "../${{ env.PACKAGE_NAME }}.zip" "${{ env.PACKAGE_NAME }}"
        shell: bash

      - name: Package playable build (Windows)
        if: ${{ matrix.platform.release && matrix.platform.os == 'windows' }}
        run: |
          $stage = "package/${{ env.PACKAGE_NAME }}"
          New-Item -ItemType Directory -Force -Path $stage | Out-Null
          Copy-Item -Path "${{ steps.targetdir.outputs.dir }}/*" -Destination $stage -Recurse -Force
          foreach ($leftover in @("logs", "context_config.toml", "compile_commands.json")) {
            Remove-Item -Path (Join-Path $stage $leftover) -Recurse -Force -ErrorAction SilentlyContinue
          }
          # The resource compiler toolchain is staged for rcli during the build. The game never
          # loads it, so it must not reach the zip.
          Get-ChildItem -Path $stage -File |
            Where-Object { $_.Name -match '(?i)slang|SPIRV-Tools|ResourceCompiler|^rcli' } |
            Remove-Item -Force
          # Only the compiled packs are read at runtime, the .slang sources are build inputs.
          $shaders = Join-Path $stage "Assets/Shaders"
          Get-ChildItem -Path $shaders -Recurse -File |
            Where-Object { $_.Extension -ne ".oxpack" } | Remove-Item -Force
          Get-ChildItem -Path $shaders -Recurse -Directory |
            Sort-Object { $_.FullName.Length } -Descending |
            Where-Object { -not (Get-ChildItem $_.FullName -Force) } | Remove-Item -Force
          Copy-Item -Path "README.md","LICENSE" -Destination $stage -Force -ErrorAction SilentlyContinue
          Compress-Archive -Path $stage -DestinationPath "${{ env.PACKAGE_NAME }}.zip" -Force
        shell: pwsh

      - name: Upload playable package
        if: ${{ matrix.platform.release }}
        uses: actions/upload-artifact@v4
        with:
          name: package-${{ env.PACKAGE_NAME }}
          path: ${{ env.PACKAGE_NAME }}.zip
          if-no-files-found: error

  release:
    needs: build
    if: ${{ startsWith(github.ref, 'refs/tags/v') }}
    runs-on: ubuntu-latest
    steps:
      - name: Download playable packages
        uses: actions/download-artifact@v4
        with:
          pattern: package-Rogylus-*
          path: packages
          merge-multiple: true

      - name: List packages
        run: ls -lh packages

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          files: packages/*.zip
          generate_release_notes: true
          body: |
            Download the zip for your platform, extract it anywhere and run the `Rogylus` executable.
            Everything the game needs ships inside the folder.

            | Platform | Download |
            | --- | --- |
            | Windows (x64) | `Rogylus-windows-x64.zip` |
            | Linux (x86_64) | `Rogylus-linux-x86_64.zip` |
            | macOS (Apple silicon) | `Rogylus-macos-arm64.zip` |

            **macOS:** the build is unsigned, so Gatekeeper quarantines it after download. Clear the
            flag once with `xattr -dr com.apple.quarantine Rogylus-macos-arm64` before running.

            **Linux:** needs a Vulkan driver for your GPU (`mesa-vulkan-drivers` or the vendor package).
